# 智能巡查系统自组网协议实现审查报告（2026-05-07）

## 1. 审查范围与基线

- 协议基线：`docs/智能巡查系统自组网协议-003.pdf`
- 软件基线：`docs/LM软件设计文档.md`（当前实现对齐目标）
- 代码范围：
  - `lib/Ad-Hoc-lib/src/adhoc_frame.c`
  - `lib/Ad-Hoc-lib/src/adhoc_sm.c`
  - `lib/Ad-Hoc-lib/src/adhoc_reply_list.c`
  - `lib/Ad-Hoc-lib/src/adhoc_data_plane.c`
  - `lib/Ad-Hoc-lib/src/adhoc_node.c`
  - `lib/Ad-Hoc-lib/src/adhoc_timing.c`
  - `app/adapters/adhoc_link_aros.c`
  - `app/tasks/ad_hoc_task.c`
  - `app/tasks/tmos_led_task.c`
- 运行证据：`docs/验证与回归清单.md` 中 run7~run12 双串口记录（含“仅复位板1”、`retry=30` 专项与四板轮换 R1~R4）。

---

## 2. 审查结论总览

| 审查项 | 现状 | 结论 |
| --- | --- | --- |
| 帧壳结构 | `32B` 固定，`Content=24B`，CRC8 覆盖 `0..30` | 符合当前软件基线 |
| Header 语义 | `msg_class + gateway_no + slot_high4` | 与 `LM软件设计文档.md` 一致 |
| A 帧时间字段 | `Content[0..3]=LMT_A(4B)` | 已实现 |
| D 帧时间字段 | `Content[0..2]=LMT_D(3B)` | 已实现 |
| 入网确认语义 | 确认统一 `flag=7 + 子节点ID` | 已实现并收口 |
| 组网确认容量 | 网关单帧最多 `5` 条确认 | 已实现 |
| D ACK 容量与格式 | 单帧最多 `3` 条，条目 `ID(4B)+LMT_D(3B)` | 已实现 |
| 去重键 | `source.node_id + LMT_D` | 已实现 |
| 网关 30s 组网窗口 | 到期 `gw_lock=1` 并停发组网广播 | 已实现，双板+四板复核通过 |
| 上级失效回退 | 连续 `3*T5` 未见上级 A 帧回退 `ST1` | 已实现，双板路径可复现 |
| 多网关候选 | `Top-K` 邻居缓存（`K=6`）+优选策略 | 已实现；应用层 `2GW` 角色映射已接入，待板测验证 |
| 数据重试上限 | 代码含 `retry_max=30` | 已实现，run11 已固化 `rpk=30/rex=11` 证据 |

**总评：** 当前实现已完成从“过渡时间戳模型”到“Content 内 `LMT_A/LMT_D`”的迁移，协议主链路（入网、确认、数据上报、ACK、窗口关闭、重试上限）与现行软件基线一致。剩余问题集中在“多网关板测覆盖不足”和“多上级高级语义收口”，而非基础协议路径缺失。

---

## 3. 与当前实现一致的关键点

### 3.1 帧模型与载荷布局

- `adhoc_frame.h` 定义 `ADHOC_FRAME_CONTENT_LEN=24`，帧总长 `32`。
- `adhoc_sm` 对 A 帧写入 `LMT_A(4B)`；`adhoc_data_plane` 对 D 帧写入 `LMT_D(3B)+Source(4B)+User(17B)`。
- `adhoc_data_ack_payload_pack` 使用每条 `7B`（`ID+LMT_D`）打包，单帧上限 `3` 条。

### 3.2 组网确认语义（Flag=7）

- 网关/已确认节点收到下级请求（`flag 0..5`）后，确认列表入队统一转为 `flag=7`。
- 子节点在 `U1/UN` 仅当收到“当前上级路径 + `flag=7` + 自身ID”才转 `C1/CN`。
- `flag=6`（已确认上级）在接收路径仅作为方向合法性信息，不触发入网确认跃迁。

### 3.3 组网窗口与状态机行为

- 网关角色在 `adhoc_sm_poll` 中：
  - 启动窗口：`gateway_network_started=1`
  - 到期锁定：`gateway_network_locked=1`
  - 锁定后停止组网广播发送
- 信标角色具备：
  - `ST1 -> U1/UN -> C1/CN` 主路径
  - 超过 `3*T5` 未收到当前上级 A 帧回退 `ST1`
  - `retry_count >= retry_max` 回退 `ST1`

### 3.4 数据面与链路观测

- 去重与 ACK 命中统一按 `source.node_id + LMT_D`。
- 转发队列支持 `T5` 周期重发、ACK 命中清理、重试耗尽报告。
- 链路适配层已加入 `TX busy` 保护（busy 时跳过 `start_rx`），并暴露 `busy/skip/error` 统计。
- LED 已加入窗口结束提示：网关 `gw_start=1 && gw_lock=1` 时“三连闪”。
- run11（`build/tmp/run11_final_retry_probe_20260506_192336/`）已命中 `retry_max=30` 路径：板2观测 `rpk=30/lrex=30/rex=11` 并多轮回退 `ST1`。
- run12（`R1~R4`）已完成四板轮换复核：`1GW+3BCN` 条件下复现窗口开闭、入网确认增长与窗口关闭后停增。

---

## 4. 当前有效差距（非过期项）

### 4.1 验证覆盖差距（高优先级）

1. **多网关场景仍缺实测**  
   四板单网关轮换（`1GW+3BCN`）已完成，且应用层网关角色映射已扩展到 `2GW` 可构建；但 `2GW+N BCN` 的方向锁定/切换策略尚无板测闭环证据。

### 4.2 协议高级语义收口差距（中优先级）

1. **`source_id_flag` 的多上级完整语义未完全收口**  
   当前实现采用“绑定号精确路径”策略，主路径可用，但多上级扩展语义仍需进一步定义与多板验证。

2. **全网统一绝对结束时刻/全局重组网策略未完全落地**  
   当前为可用的本地窗口与回退策略，已满足现阶段联调，但“全网统一结束对齐”仍待后续协议策略收口。

### 4.3 工程层说明（低优先级）

- `lib/Ad-Hoc-lib/include/adhoc_h.md` 明确标注为历史摘录，内容保留旧常量（如 `user[18]`、`ACK=4`），不应作为当前接口依据。当前接口以 `.h` 实头文件与本报告为准。
- run11 使用了测试场景构造（`ADHOC_TASK_GATEWAY_T2_US_OVERRIDE` + 临时确认丢弃宏）；常规构建必须保持测试宏关闭，避免污染日常联调结论。

---

## 5. 与旧报告的替代关系

本报告替代旧版 `protocol-audit-report.md`，并移除了以下**已过期或不再成立**结论：

- “当前代码仍是帧级 `Timestamp(2B)+Content(22B)`”  
  → 现状已迁移为 `Content(24B)` + `LMT_A/LMT_D`。
- “D ACK 仍是 `ID+seq_no` / 单帧 4 条”  
  → 现状为 `ID+LMT_D` / 单帧 3 条。
- “入网确认未使用 `flag=7`”  
  → 现状已完成 `flag=7` 收口。

---

## 6. 后续建议（按优先级）

1. **推进多网关联调**：基于现有 `2GW` 构建能力执行 `2GW+N BCN`，验证 Top-K 选择、方向锁定释放与切换稳定性。
2. **推进确认满载压测**：验证“单帧最多5条组网确认”的容量上限与补0行为。
3. **固化四板回归流程**：将 run12 R1~R4 脚本化，形成可重复验收入口。
4. **在上述验证完成后，再进入全网统一结束时刻策略收口**，避免过早增加协议复杂度。

---

## 7. 增量审查结论（2026-05-07）

### 7.1 新增板测证据

- 接线：`COM8=GW1`、`COM3=节点1(BCN)`、`COM11=GW2虚拟串口`，节点2仅上电。
- 复位方式：`GW1` 走 OpenOCD `reset run`，`GW2` 走 `COM11` 文本命令 `GW2RST`。
- 证据文件：`logs/node1_verify_20260507_165750.log`。
- 关键结果：
  - 节点1出现 `ST1 -> U1 -> C1`。
  - 节点1数据确认从 `sub=1/141, ack=2` 增长到 `sub=34/175, ack=74`。
  - `rex=0`，本轮未命中“重试耗尽导致上报失败”。

### 7.2 观测口径收口

- 网关侧 `adhoc data sub/ack` 在当前实现中是“本节点数据提交与确认统计”，不用于判断“子节点是否被确认”。
- “节点数据上报闭环是否成立”应以节点侧统计为准：`data sub` 与 `ack` 是否增长。
- 基于上述口径，本阶段“可入网 + 可确认 + 可持续上报”结论成立。
