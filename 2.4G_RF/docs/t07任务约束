
---
> 历史约束说明（保留参考）：本文件为早期方案记录，当前已不作为主线实现基线。  
> 现行基线请以 `docs/t07任务约束评估.md`、`docs/协议状态转移深度拆解.md` 和 `docs/任务分解技术书.md` 为准。

### T07 任务分解：全网时间对齐与显式传播

#### 1. 帧协议层约束 (Frame Protocol Constraints)

**文件：** `adhoc_frame.h`
**目标：** 定义时间戳在 A 类帧 Payload 中的固定位置。

*   **约束 1.1：定义 Epoch 时间戳索引**
    *   **位置：** 在 `#define ADHOC_FRAME_PAYLOAD_LEN` 之后。
    *   **代码：**
        ```c
        // A类帧Payload中，网络纪元时间戳(Epoch)的起始索引
        #define ADHOC_PAYLOAD_EPOCH_IDX 0u
        // Epoch时间戳固定占用4字节
        #define ADHOC_PAYLOAD_EPOCH_LEN 4u
        ```
    *   **说明：** 这意味着 A 类帧的 Payload[0..3] 永远存放网关广播的绝对起始时间（单位：毫秒）。

*   **约束 1.2：Payload 偏移修正**
    *   **说明：** 由于前 4 字节被时间戳占用，A 类帧中原本紧跟在时间戳后的“确认列表”（Confirm List）或“邻居信息”必须从索引 4 开始存放。这需要同步修改 `reply_list` 模块的打包偏移量。

---

#### 2. 网关侧逻辑约束 (Gateway Logic Constraints)

**文件：** `adhoc_sm.c`
**目标：** 网关在发送 A 类帧时，必须填入绝对起始时间。

*   **约束 2.1：网关初始化时间基准**
    *   **触发点：** `adhoc_sm_init` 或 `adhoc_sm_set_role` (当角色切换为 GATEWAY 时)。
    *   **逻辑：** 如果 `gateway_network_start_us` 为 0，则初始化为当前时间戳。
    *   **伪代码约束：**
        ```c
        if (sm->cfg.role == ADHOC_SM_ROLE_GATEWAY && sm->gateway_network_start_us == 0) {
            sm->gateway_network_start_us = now_us; // now_us 由上层注入
            sm->gateway_network_end_us = now_us + sm->cfg.network_window_us;
        }
        ```

*   **约束 2.2：A 帧打包逻辑 (Injection)**
    *   **函数：** `adhoc_sm_emit_gateway_beacon` (或类似构建 A 帧的函数)。
    *   **逻辑：** 在构建 `frame->payload` 时，将 `gateway_network_start_us` (转换为毫秒) 写入前 4 字节。
    *   **伪代码约束：**
        ```c
        // 假设 frame 是待发送的帧结构
        uint32_t epoch_ms = sm->gateway_network_start_us / 1000; // 转换为毫秒防止溢出
        
        // 强制写入 Payload 前 4 字节
        frame->payload[0] = (epoch_ms >> 24) & 0xFF;
        frame->payload[1] = (epoch_ms >> 16) & 0xFF;
        frame->payload[2] = (epoch_ms >> 8)  & 0xFF;
        frame->payload[3] = epoch_ms        & 0xFF;
        
        // 注意：后续的 Reply List 打包必须从 &frame->payload[4] 开始
        // adhoc_reply_list_build_confirm_payload(..., &frame->payload[4], ...);
        ```

---

#### 3. 信标侧逻辑约束 (Beacon Logic Constraints)

**文件：** `adhoc_sm.c`
**目标：** 信标必须放弃“本地观测时间”，转而使用“网关广播时间”。

*   **约束 3.1：时间戳解析 (Parsing)**
    *   **函数：** `adhoc_sm_on_rx` (处理 `ADHOC_MSG_CLASS_A` 分支)。
    *   **逻辑：** 从收到的帧 `payload[0..3]` 提取时间戳，并计算全网统一的结束时间。
    *   **伪代码约束：**
        ```c
        // 仅在收到 A 类帧时解析
        if (rx_event->msg_class == ADHOC_MSG_CLASS_A) {
            // 1. 解析 Epoch 毫秒
            uint32_t epoch_ms = (frame->payload[0] << 24) | 
                                (frame->payload[1] << 16) | 
                                (frame->payload[2] << 8)  | 
                                frame->payload[3];
            uint64_t epoch_us = (uint64_t)epoch_ms * 1000;
            
            // 2. 更新 Candidate (邻居缓存)
            // 获取当前配置的窗口长度 (network_window_us)
            uint32_t window_us = sm->cfg.network_window_us; 
            
            // 3. 【核心约束】强制覆盖本地时间
            // 无论信标是什么时候收到的第一个包，它的结束时间都必须是 epoch_us + window_us
            candidate->network_end_us = epoch_us + window_us;
            
            // 标记该 Candidate 已经过时间对齐
            candidate->flags |= CANDIDATE_FLAG_EPOCH_SYNCED; 
        }
        ```

*   **约束 3.2：状态机判定修正 (Alignment)**
    *   **函数：** `adhoc_sm_poll`
    *   **逻辑：** 修改“组网窗口是否结束”的判定逻辑。
    *   **旧逻辑（废弃）：** `now_us > (candidate->first_seen_us + window_us)`
    *   **新逻辑（强制执行）：**
        ```c
        // 判定组网窗口是否关闭
        if (sm->candidate.active && 
            sm->candidate.network_end_us > 0 && 
            now_us >= sm->candidate.network_end_us) {
            
            // 执行锁定逻辑 (进入 ST0 或 UN 锁定态)
            adhoc_sm_enter_locked_state(sm);
        }
        ```

---

### 关键风险与应对 (基于头文件分析)

1.  **Payload 空间冲突 (High Risk)**
    *   **现状：** `ADHOC_FRAME_PAYLOAD_LEN` 固定为 24 字节。
    *   **风险：** 如果 A 类帧原本的“确认列表”数据长度超过 20 字节（24 - 4 时间戳），数据将被截断。
    *   **应对：** 必须在 `adhoc_reply_list_build_confirm_payload` 中限制最大打包数量，确保 `count * sizeof(item) <= 20`。

2.  **时间回绕 (Overflow)**
    *   **现状：** 使用 4 字节 (uint32_t) 传输毫秒级时间戳。
    *   **风险：** 49.7 天后回绕。
    *   **应对：** 在解析时进行简单的差值判断，或者在协议中约定使用“距离网关启动的相对毫秒数”而非“绝对 Unix 时间”，这样只要组网窗口小于 49 天即可。

3.  **网关切换 (Handover)**
    *   **现状：** 信标可能先收到网关 A，后收到网关 B。
    *   **逻辑：** 信标应始终跟随 `network_end_us` 最晚的那个网关（或者根据 RSSI/Level 策略选择主网关，但时间必须严格对齐主网关的 Epoch）。

