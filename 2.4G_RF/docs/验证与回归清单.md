# 自组网协议验证与回归清单（v0.1）

## 1. 目的

本文档将 `docs/设计文档（初稿）.md` 第 9 节“一致性核对清单”转为可执行验证项，作为 `T10` 的最小入口。

当前策略：

- 先保证“可观测、可构建、可联调”。
- 再逐项补齐板侧联调记录与主机侧/脚本侧验证。
- 若某项暂时只能人工验证，必须明确观测点与通过标准。

## 2. 验证分层

### 2.1 构建验证

- 命令：`xmake`
- 命令：`xmake f -m release && xmake`
- 目标：确认默认主路径可在 `debug/release` 下稳定构建。

### 2.2 板侧运行观测

- 观测入口：`serial_upload_task`
- 观测入口：`display_task`
- 观测入口：`tmos_led_task`
- 目标：确认 `ad_hoc_task`、链路适配层与数据面具备统一状态源。

### 2.3 协议行为验证

- 观测对象：组网消息 `A`、数据消息 `D`、网关确认、数据确认、重试回退。
- 目标：确认协议细节与 `docs/智能巡查系统自组网协议-003.pdf` 及 `docs/LM软件设计文档.md` 的本轮时间戳修订一致。

## 3. 核对项映射

| 核对项 | 验证方式 | 观测点/文件 | 当前状态 |
| --- | --- | --- | --- |
| `T5=T1+T2`、`T6=T3+T4`、`n=T5/T1`、`m=T6/T3` | 编译期/运行期配置检查 | `lib/Ad-Hoc-lib/src/adhoc_timing.c` | 已覆盖 |
| 报文固定 `32B`、CRC8 覆盖正确 | 编码/解码与校验路径检查 | `lib/Ad-Hoc-lib/src/adhoc_frame.c`、`lib/Ad-Hoc-lib/src/adhoc_crc8.c` | 已覆盖 |
| 时间戳位于 `Content` 内：`A=LMT_A(4B)`、`D=LMT_D(3B)` | 编码/解码与载荷布局检查 | `lib/Ad-Hoc-lib/src/adhoc_frame.c`、`lib/Ad-Hoc-lib/src/adhoc_data_plane.c`、`lib/Ad-Hoc-lib/src/adhoc_reply_list.c` | 已覆盖 |
| `A/D` 类别、网关号、时隙号位定义一致 | 编解码检查 | `lib/Ad-Hoc-lib/src/adhoc_frame.c` | 已覆盖 |
| 级别奇偶与时隙奇偶一致 | 收包校验与时序检查 | `lib/Ad-Hoc-lib/src/adhoc_timing.c`、`lib/Ad-Hoc-lib/src/adhoc_node.c` | 已覆盖 |
| 域号检查与 ID 分界规则正确 | 收包过滤与配置检查 | `lib/Ad-Hoc-lib/src/adhoc_node.c` | 已覆盖 |
| ID 标志 `0..7` 语义严格一致 | 组网应答逻辑检查 | `lib/Ad-Hoc-lib/src/adhoc_reply_list.c`、`lib/Ad-Hoc-lib/src/adhoc_sm.c` | 已完成代码收口/双板复核通过 |
| 1 级与 n 级最多 `30` 周期重试 | 状态机联调 | `lib/Ad-Hoc-lib/src/adhoc_sm.c`、`app/tasks/ad_hoc_task.c`、`app/tasks/serial_upload_task.c` | 已完成 run11 命中（`rpk=30/rex=11`） |
| 网关组网时间窗到期停发 `A V=0` | 状态机联调 | `lib/Ad-Hoc-lib/src/adhoc_sm.c`、`app/tasks/serial_upload_task.c` | 已双板与四板轮换复核 |
| 信标方向结束时刻到期后锁定停发 | 状态机联调 | `lib/Ad-Hoc-lib/src/adhoc_sm.c`、`app/tasks/serial_upload_task.c` | 已双板验证（run7/run8）/四板轮换复核通过 |
| 连续 `3*T5` 未收到当前上级 `A` 帧后回退 `ST1` | 状态机联调 | `lib/Ad-Hoc-lib/src/adhoc_sm.c`、`app/tasks/serial_upload_task.c` | 已双板复核（run7/run8）/四板轮换复核通过 |
| 组网确认单帧最多 `5` 个下级，确认标志为 `flag=7` | 打包检查 | `lib/Ad-Hoc-lib/src/adhoc_reply_list.c` | 已完成代码收口/主路径联调通过（满载5条待压测） |
| 网关数据应答单帧最多 `3` 条 `ID+LMT_D` | 数据面打包检查 | `lib/Ad-Hoc-lib/src/adhoc_data_plane.c` | 已覆盖 |
| 多网关取舍后只响应所选方向 | 状态机联调 | `lib/Ad-Hoc-lib/src/adhoc_sm.c`、`app/tasks/serial_upload_task.c` | Windows 仿真已覆盖/待实板板测 |
| 重组网到期回退 `ST1` | 状态机联调 | `lib/Ad-Hoc-lib/src/adhoc_sm.c` | Windows 仿真已验证本地计时/全局对齐待补充 |
| `adhoc sm` 运行态字段可观测 `gw_left/gw_lock/up_age/net_left` | 串口观测 | `app/tasks/serial_upload_task.c`、`app/tasks/ad_hoc_task.c` | 已覆盖 |

## 4. 当前联调入口

### 4.1 串口日志

- `uplink stat ...`：传感器快照摘要
- `adhoc stat ...`：协议主状态摘要
- `adhoc data ...`：数据提交与确认结果
- `adhoc link ...`：链路层 busy/fail/RSSI/TC 观测
- `adhoc sm ...`：状态机运行态观测（`st/lvl/retry/up_gw/up_no/up_age/gw_left/gw_lock/net_left`）

### 4.2 OLED

- 角色/域号/网关号
- 节点 ID
- `TX/RX`
- `SUB/ACK`
- `LNK busy/error`

### 4.3 LED

- 基于 `ad_hoc_task` 的 `rx_cnt`、`tx_report_acked`、`data_submit_ok` 活跃度判定链路存活。
- 网关窗口结束提示：当 `gw_start=1 && gw_lock=1` 时切换为固定节拍“三连闪”提醒（用于板侧快速识别时间窗已关闭）。

### 4.4 单板验证记录（2026-04-26）

- 条件：`1` 块板、串口 `COM8/115200`、网关角色。
- 操作：烧录当前 debug 固件后，连续读取串口约 `40s`。
- 结果：
  - `adhoc sm` 中 `gw_left` 持续倒计时，并在约 `30s` 后归零。
  - `gw_lock` 由 `0` 翻转为 `1`，表明网关组网窗口已关闭。
  - `tx` 与 `link tx req` 计数在 `gw_lock=1` 后停止增长，符合“时间窗到期停发 `A V=0`”预期。
- 结论：当前已具备网关侧 `T07` 时间窗停发的单板硬件证据；信标锁定、多网关、上级失效回退仍待双板/多板继续板测。

### 4.5 双板验证记录（2026-04-26）

- 条件：板1为网关、板2为 `RF_TG_ID=1` 信标，仅观测板2 `COM3/115200`。
- 结果：
  - 板2从 `ST1` 进入 `C1`：`st=3`、`lvl=1`、`up_id=1`、`net_act=1`。
  - `adhoc data ack` 从 `4` 增长到 `11`，同时 `tx/rx` 计数持续增长，说明最小入网与数据上报 ACK 闭环已跑通。
  - 随后板2又回退到 `ST1`：`st=1`、`up_id=0`、`net_act=0`。
- 结论：
  - 可确认“最小入网 + 数据 ACK”主路径已具备真实双板证据。
  - 回退现象与“连续 `3*T5` 未收到上级 `A` 帧则回退 `ST1`”一致，但因本轮未同步采集板1侧状态，仍需后续复核其直接触发原因。

### 4.6 双串口复核记录（问题定位，2026-04-26）

- 条件：同时观测板2 `COM3` 与板1 `COM8`。
- 结果：
  - 板2复位窗口内可短暂进入 `C1`，随后再次回退 `ST1`。
  - 板1串口可确认 `role=GW`、`gw_lock=1`，并观测到链路层统计 `tx=16 / tx_req=477 / busy=461`。
- 结论：
  - 当前回退现象更偏向“网关侧有效广播过稀，超过 `3*T5` 后触发板2上级失效回退”。
  - 后续应优先排查 `adhoc_link_aros` / AROS-RF 发送 busy 原因，而不是继续把问题归因于 `T07` 状态机条款缺失。

### 4.7 双串口复核记录（修复验证，2026-04-26）

- 修复项：
  - 在 `app/adapters/adhoc_link_aros.c` 的 `adhoc_link_aros_start_rx_impl` 增加 `TX busy` 保护：`arf_isTxBusy()!=0` 时跳过 `RxStart`，并累计 `start_rx_skip_txbusy_cnt`。
- 结果（窗口开启阶段，历史 after-fix 采集）：
  - 板1：`gw_lock=0`，`tx=183 / tx_req=185 / busy=2`（busy 显著下降）。
  - 板2：`ST1 -> C1`（`st=3`、`up_id=1`），`adhoc data ack` 从 `0` 连续增长到 `10`。
- 结果（窗口关闭阶段，run2 同步采集）：
  - 板1：`gw_lock=1`，`tx=494 / tx_req=497 / busy=3`，广播处于关闭后稳定态。
  - 板2：保持 `ST1`，`ack` 维持 `18` 不再增长，符合“组网窗口关闭后不再新入网”预期。
- 结论：
  - 旧问题（高 busy 导致间歇失联）已被定位并得到轻量修复验证，当前 `T07` 可进入“继续补测多网关/多信标策略”的阶段。

### 4.8 双串口复核记录（run7 复位节拍验证，2026-04-26）

- 条件：
  - 采集方式：双串口自动重连采集（run7），板2=`COM3`，板1=`COM8`。
  - 复位节拍：先复位板2，约 `3s` 后复位板1。
- 结果（板1，网关）：
  - 先出现串口重枚举：`COM8` 在日志中可见 `FileNotFoundError` 后自动重连。
  - 重连后进入新一轮时间窗：`gw_lock=0, gw_left=29030ms`。
  - 约 `30s` 后窗口关闭：`gw_lock=1, gw_left=0ms`，同时 `tx=497/508, busy=11`，仍保持低 busy 稳态。
- 结果（板2，信标）：
  - 复位后统计归零：`tx=0, rx=0`，随后重新进入组网。
  - 可见 `ST1 -> C1`：`st=3, lvl=1, up_id=1`。
  - `ack` 从 `1` 持续增长到 `28`，随后随窗口结束回到 `ST1` 并停止继续增长。
- 结论：
  - “顺序复位 + 自动重连采集”流程可稳定复现，`T07` 的“窗口开启可入网、窗口关闭停入网”行为再次得到双串口证据。
  - run7 期间发现串口脚本在 Windows `GBK` 控制台下可能触发 `UnicodeEncodeError`，已在 `scripts/serial_reader.py` 增加安全输出回退，避免采集中断。

### 4.9 双串口复核记录（run8 复位节拍验证，2026-04-27）

- 条件：
  - 采集方式：双串口自动重连采集（run8），板2=`COM3`，板1=`COM8`。
  - 复位节拍：先复位板2，约 `3s` 后复位板1。
- 结果（板1，网关）：
  - 可见串口重枚举（`ClearCommError` + `FileNotFoundError`）后自动恢复。
  - 重连后重新进入时间窗：`gw_lock=0, gw_left=29031ms`。
  - 约 `30s` 后回到窗口关闭：`gw_lock=1, gw_left=0ms`，链路统计 `tx=500/505, busy=5`，保持低 busy。
- 结果（板2，信标）：
  - 复位后先清零再入网：`tx=0, rx=0` -> `ST1 -> C1(st=3, up_id=1)`。
  - `ack` 从 `2` 增长到 `24`，随后回到 `ST1`，符合窗口切换阶段行为。
  - run8 中统计到 `st3=20`、`st2=2`，说明存在短暂重试态但主路径可持续恢复。
- 结论：
  - run8 与 run7 结论一致：`T07` 的“窗口开启可入网 + 窗口关闭停入网”在双串口与顺序复位条件下可重复复现。
  - 串口脚本兼容性修复继续有效：run8 两路 `.err` 均为空，未再出现 `UnicodeEncodeError` 中断。

### 4.10 双板阶段收口（2026-04-27）

- 已完成（双板可完成项）：
  - 网关时间窗闭环：`gw_lock=0 -> gw_lock=1` 可重复复现（run7/run8）。
  - 信标方向行为：`ST1 -> C1 -> ST1` 可重复复现，且窗口阶段 `ack` 持续增长。
  - 上级失效回退：已观测到 `ST2` 短暂重试与回退 `ST1`，并有板1同步日志可对齐。
- 仍待后续：
  - 多网关取舍与“5条确认满载”压测仍待后续阶段补齐。
  - “全网统一绝对结束时刻”策略仍待后续协议阶段收口。

### 4.11 双串口复核记录（run9 重试路径探测，2026-04-27）

- 条件：
  - 采集方式：双串口自动重连采集（run9），板2=`COM3`，板1=`COM8`。
  - 操作：采集启动后仅复位板1（OpenOCD `reset run`），不复位板2。
- 结果（板1，网关）：
  - 再次完整复现时间窗：`gw_lock=0, gw_left=29031ms` -> `gw_lock=1, gw_left=0ms`。
  - 窗口关闭时链路统计 `tx=503/505, busy=2`，保持低 busy。
- 结果（板2，信标）：
  - 观测到重试路径活跃：`ST2(retry=3)` -> `C1` -> `ST1` -> `ST2(retry=5)`。
  - 本轮 `ack` 由 `52` 增至 `60`，说明窗口开启阶段仍可持续完成数据确认。
  - 本轮未命中 `retry=30`，最大观测重试计数为 `5`。
- 结论：
  - 双板已可稳定复现“重试路径存在 + 回退路径存在 + 窗口闭环存在”。
  - `30` 周期上限验证已进入“目标已明确、触发样例待固化”阶段。

### 4.12 双串口复核记录（run10 仅复位板1，2026-05-05）

- 条件：
  - 板1=`COM8`（网关，可通过烧录/复位触发新窗口），板2=`COM3`（信标，不复位）。
  - 双串口自动重连采集先启动，再执行板1复位。
- 结果（板1，网关）：
  - 复位后再次进入时间窗：`gw_lock=0`，`gw_left` 从约 `27s` 持续递减。
  - 到期后再次关闭：`gw_lock=1`、`gw_left=0ms`。
- 结果（板2，信标）：
  - 在板1新窗口内可见 `ST1 -> C1`，`ack` 连续增长（本轮片段观测 `1 -> 6`）。
  - 板1窗口关闭后回到 `ST1`，且 `ack` 保持不再增长（本轮片段观测稳定在 `60`）。
- 结论：
  - “仅复位板1”即可稳定复现“窗口开启可入网 + 窗口关闭停入网”。
  - 双板主路径验证不依赖板2复位控制，可直接复用到后续回归流程。

### 4.13 `retry=30` 专项验证结果（run11，2026-05-06）

- 目标：
  - 在双板条件下稳定触发 `U1/UN -> retry_max -> ST1`，补齐 `30` 周期重试上限板测证据。
- 构造方式（测试专用）：
  - 网关侧启用 `ADHOC_TASK_GATEWAY_T2_US_OVERRIDE=100`，压缩 `T2` 收集窗口。
  - 临时启用 `ADHOC_TEST_DROP_JOIN_CONFIRM=1`，验证时丢弃入网确认列表（宏默认 `0`，常规构建不生效）。
- 结果证据（目录：`build/tmp/run11_final_retry_probe_20260506_192336/`）：
  - 板2（`board2_com3.out.txt`）统计：`rpk=30`、`lrex=30`、`rex` 递增至 `11`，状态分布 `st1=36/st2=29`。
  - 板2出现多轮 `U1/UN` 重试并回退 `ST1`，满足“重试耗尽后回退”的目标路径。
  - 板1（`board1_com8.out.txt`）可见 `gw_lock=0 -> gw_lock=1`，窗口闭环仍正常。
- 结论：
  - `retry_max=30` 的协议逻辑已被双板实测命中并固化证据。
  - 本专项属于“测试场景构造验证”，常规联调需保持上述测试宏关闭。

### 4.14 四板轮换复核（R1：`GW + B1`，2026-05-06）

- 证据目录：`build/tmp/run12_r1_flashreset_20260506_195842/`
- 结果（网关 `COM8`）：
  - 观测到 `gw_lock=0 -> 1`，`gw_left` 从约 `29028ms` 归零。
- 结果（信标 `COM3`，B1）：
  - 观测到 `ST1 -> C1`（`st=3`）入网确认路径。
  - `ack` 从 `140` 增长到 `237`。
- 结论：
  - R1 满足“窗口开启可入网 + 窗口关闭后停增”的最小闭环。

### 4.15 四板轮换复核（R2：`GW + B2`，2026-05-06）

- 证据目录：`build/tmp/run12_r2_flashreset_20260506_200419/`
- 结果（网关 `COM8`）：
  - 观测到 `gw_lock=0 -> 1`，`gw_left` 最大约 `29030ms`。
- 结果（信标 `COM3`，B2）：
  - 观测到 `st=3`（确认态），状态集合含 `st={1,3,4,5}`。
  - `ack` 从 `0` 增长到 `178`。
- 结论：
  - R2 满足“网关开窗 + 信标入网确认 + 数据确认增长”。

### 4.16 四板轮换复核（R3：`GW + B3`，2026-05-06）

- 证据目录：`build/tmp/run12_r3_flashreset_20260506_200904/`
- 结果（网关 `COM8`）：
  - 观测到 `gw_lock=0 -> 1`，`gw_left` 最大约 `29025ms`。
- 结果（信标 `COM3`，B3）：
  - 观测到 `st=3`（确认态），`ack` 从 `0` 增长到 `84`。
  - 后段出现 `rpk=30`、`rex=2`，并回到 `ST1`，说明重试/回退路径在四板环境可达。
- 结论：
  - R3 满足“可入网可确认”，并补充了真实干扰下重试回退样例。

### 4.17 四板轮换复核（R4：仅复位网关，四板同上电，2026-05-06）

- 证据目录：`build/tmp/run12_r4_gateway_only_20260506_201207/`
- 条件：
  - 网关板复位（通过烧录命令触发 `reset run`），信标不复位。
  - 四块板保持同时上电。
- 结果（网关 `COM8`）：
  - 观测到 `gw_lock=0 -> 1`，`gw_left` 最大约 `29030ms`。
- 结果（信标观测口 `COM3`）：
  - `ack` 在开窗期从 `84` 增到 `89`，在 `gw_lock=1` 后保持 `89` 不再增长。
  - 末态回到 `ST1`，保留 `rpk=30/rex=11` 历史值。
- 结论：
  - R4 满足“仅复位网关即可复现窗口开闭，且窗口关闭后确认计数停增”。

### 4.18 2GW 固件构建基线（run13 预置）

- 代码基线：`app/tasks/ad_hoc_task.c` 已支持双网关角色映射，`xmake.lua` 已支持第二网关参数。
- 默认兼容模式（单网关）：
  - `xmake f -c --rf_tg_id=0 --adhoc_role=gw --adhoc_gw_no=0`
- 第二网关固件示例（`tg_id=2` 承担 `gw_no=1`）：
  - `xmake f -c --rf_tg_id=2 --adhoc_role=gw --adhoc_gw_no=1`
- 信标固件示例（保持双网关映射开启）：
  - `xmake f -c --rf_tg_id=1 --adhoc_role=bcn --adhoc_gw_no=0`
  - `xmake f -c --rf_tg_id=3 --adhoc_role=bcn --adhoc_gw_no=0`
- 构建结果：上述配置均已本地编译通过（debug）。

## 4.19 双网关+节点口径复核（2026-05-07）

- 接线条件：
  - `COM8`：网关1（可 OpenOCD 复位）
  - `COM3`：节点1（`BCN`，`node=1000001`）
  - `COM11`：网关2虚拟串口（发送 `GW2RST` 软复位）
  - 节点2：仅上电
- 复位节拍：
  - 先对 `GW1(COM8)` 执行 `reset run`
  - 再对 `GW2(COM11)` 发送 `GW2RST\r\n`
- 节点侧关键观测（`logs/node1_verify_20260507_165750.log`）：
  - 入网状态跃迁：`ST1 -> U1(st=2,lvl=1) -> C1(st=3,lvl=1)`
  - 上报与确认增长：`sub=1/141, ack=2` 逐步增长至 `sub=34/175, ack=74`
  - 期间 `rex=0`，说明未出现“重试耗尽导致的上报失败”
- 口径结论：
  - 网关日志中的 `adhoc data sub/ack=0` 不能用于判断“节点是否已被确认”，该计数在当前实现里是**本节点数据提交/确认计数**。
  - 判断“数据上报闭环”应优先看节点侧：`data sub` 与 `ack` 是否持续增长。
- 当前阶段结论：
  - 节点侧已形成“可入网 + 可确认 + 可持续上报”的实测证据，当前阶段验收通过。

## 4.20 Windows 仿真回归接受（2026-05-11）

- 证据来源：
  - `lib/Ad-Hoc-lib/test/验证与回归清单.md`
  - `lib/Ad-Hoc-lib/test/README.md`
  - `lib/Ad-Hoc-lib/docs/软件设计文档.md`
- 接受范围（可作为协议层证据）：
  - 场景一/二：确认 `ST1/U1/UN/C1/CN` 主路径、`flag=7` 入网确认、多级转发与网关 ACK 主链路符合设计预期。
  - 场景三/七：确认 `Top-K` 候选选择、方向锁定、双网关并行存在与三级混合拓扑在仿真环境下可运行。
  - 场景四/八：确认 `3*T5` 上级失效回退与 `regroup_interval_us` 本地计时回退均可触发。
  - 场景五/六/八：确认去重、`retry_max=30`、高频数据、16 节点压力与 10 分钟稳定性在仿真环境下成立。
- 接受边界（不能替代实板）：
  - 仿真信道为单机共享虚拟时间与理想化拓扑，不覆盖真实 RF busy、频点/信道切换、板级时钟漂移、中断抢占与 TMOS 调度抖动。
  - 因此“多网关协议逻辑已覆盖”可以成立，但“多网关实板链路表现已验收”不能成立。
- 当前阶段结论：
  - 可将 Windows 仿真结果接受为 `T07/T10` 的协议层补充证据；
  - 后续实板工作重点从“证明协议逻辑是否成立”收缩为“验证真实链路层与射频条件下是否仍保持同样行为”。

## 4.21 Windows 全场景自动回归（2026-05-19）

- 入口脚本：`lib/Ad-Hoc-lib/test/build_and_run.bat all`
- 自动化脚本：`lib/Ad-Hoc-lib/test/run_all_scenarios.ps1`
- 场景范围：`SCENARIO_MODE=0..13`
- 汇总报告：`lib/Ad-Hoc-lib/test/logs/regression_summary_20260519-175903.md`
- 结果：`14/14 PASS`
- 关键统计：
  - 高压场景：`S5 ACKED=1269, RETRY_EXHAUSTED=388`
  - 长稳态场景：`S6 elapsed=607.4s`
  - 链式深度场景：`S8/S9` 分别为 `ACKED=361/781`
- 结论：当前协议库在 Windows 多线程仿真下已完成一轮全场景自动回归，可作为后续实板联调前的基线检查。

## 5. 下一步建议

1. 多网关实板板测：继续执行 `2GW + N BCN`，补齐真实 RF 条件下的方向锁定与切换证据。
2. 组网确认满载压测：重点补齐“单帧最多 `5` 条确认”的实板并发验证与边界日志。
3. 回归流程收口：把 `build_and_run.bat all` 纳入每次协议改动后的固定门禁，并沉淀失败场景复现模板。
4. 构建基线约束：常规构建保持 `ADHOC_TASK_GATEWAY_T2_US_OVERRIDE=0` 且 `ADHOC_TEST_DROP_JOIN_CONFIRM=0`。
